name: Terraform Workflow
 
on:
  push:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Terraform
        run: |
          git config --global url."https://${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}@github.com/Samaysinghai/newone.git".insteadOf "https://github.com/Samaysinghai/newone.git"
          terraform init
          # Other Terraform commands...
